
    const umn = (a, b) => a * b;
    const vic = (a, b) => a - b;
    const sum = (a, b) => a + b;

        const del = (a, b) =>{
            if (a == null & b == 2) {
                return null;
            }
            a / b;
        };


        module.exports = {
            sum: sum,
            del:del,
            umn:umn,
            vic:vic,
        };
